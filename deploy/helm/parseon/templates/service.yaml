apiVersion: v1
kind: Service
metadata:
  name: {{ include "parseon.fullname" . }}
  labels:
    {{- include "parseon.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "parseon.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
